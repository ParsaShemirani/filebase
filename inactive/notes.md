

## Current state
- Working collection, label, description system
- Embeddings working and search is great
- Retrieve currently copys everything to the terminal

## Next moves
- Solidiy filename logic, if filenames must be unique at all, unique within a collection
- Make logic to copy a collection and its child collections
- Logic to show collection trees
- Maybe I can have a top level 'austria_trip' collection, with many child collections
- I can retrieve it in the manner that I retrieve the top level, and all of the children's files get retrieved in the same directory, or within the original structure. Then, collections must have unique names within a parent collection, but not necessarily within the global collection namespace
